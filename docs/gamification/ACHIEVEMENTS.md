# 成就系統設計文檔

## 系統概述

成就系統是一個遊戲化學習機制，通過追蹤用戶的學習進度並獎勵完成特定任務，提升學習動機和參與度。

## 核心功能

### 1. 成就追蹤（Achievement Tracking）
- 自動檢測用戶完成的學習任務
- 使用 localStorage 持久化成就數據
- 支持多種成就觸發條件

### 2. 成就彈窗（Achievement Popup）
- 解鎖成就時顯示動畫彈窗
- 顯示成就圖標、名稱、描述
- 自動消失或手動關閉
- 支持音效（可選）

### 3. 進度追蹤（Progress Tracking）
- 追蹤每個模塊的完成狀態
- 統計總體學習進度
- 可視化進度環形圖

### 4. 排行榜（Leaderboard）
- 本地排行榜（基於成就點數）
- 顯示完成時間、成就數量
- 支持重置和導出數據

## 數據結構

### Achievement 對象
```javascript
{
  id: 'first-game',              // 唯一標識
  category: 'games',             // 分類：games, tutorials, projects
  name: '首次遊戲',              // 成就名稱
  description: '完成第一個遊戲', // 描述
  icon: '🎮',                    // 圖標
  points: 10,                    // 獎勵點數
  condition: {                   // 解鎖條件
    type: 'complete',            // 類型：complete, count, time, combo
    target: 'whack-a-mole',      // 目標
    value: 1                     // 目標值
  },
  unlocked: false,               // 是否已解鎖
  unlockedAt: null,              // 解鎖時間戳
  rarity: 'common'               // 稀有度：common, rare, epic, legendary
}
```

### User Progress 對象
```javascript
{
  userId: 'local-user',          // 用戶ID（本地固定）
  totalPoints: 0,                // 總點數
  achievements: [],              // 已解鎖成就ID數組
  modules: {                     // 模塊完成記錄
    'whack-a-mole': {
      completed: true,
      firstCompletedAt: timestamp,
      lastCompletedAt: timestamp,
      playCount: 5,
      bestScore: 150
    }
  },
  stats: {                       // 統計數據
    gamesCompleted: 0,
    tutorialsCompleted: 0,
    projectsCompleted: 0,
    totalPlayTime: 0,             // 毫秒
    totalSessions: 0
  }
}
```

## 成就列表設計

### 遊戲類成就（Games）

#### 入門成就
1. **首次遊戲** 🎮
   - 完成任意一個遊戲
   - 點數：10
   - 稀有度：common

2. **打地鼠新手** 🦫
   - 完成打地鼠遊戲並得分 ≥ 100
   - 點數：15
   - 稀有度：common

3. **拼圖入門** 🧩
   - 完成拖放拼圖
   - 點數：20
   - 稀有度：common

4. **貪吃蛇學徒** 🐍
   - 在貪吃蛇中得分 ≥ 50
   - 點數：15
   - 稀有度：common

5. **表單專家** 📝
   - 完成表單驗證大師的所有驗證
   - 點數：25
   - 稀有度：rare

6. **音樂新手** 🎹
   - 在音樂按鍵中彈奏一首完整旋律
   - 點數：20
   - 稀有度：common

#### 進階成就
7. **遊戲達人** 🏆
   - 完成所有 5 個遊戲
   - 點數：50
   - 稀有度：rare

8. **打地鼠大師** 🦫✨
   - 打地鼠得分 ≥ 200
   - 點數：30
   - 稀有度：rare

9. **貪吃蛇高手** 🐍✨
   - 貪吃蛇得分 ≥ 100
   - 點數：35
   - 稀有度：rare

10. **速度之王** ⚡
    - 30 秒內完成拼圖
    - 點數：40
    - 稀有度：epic

### 教程類成就（Tutorials）

11. **事件學徒** 📚
    - 完成任意一個教程
    - 點數：15
    - 稀有度：common

12. **傳播專家** 📡
    - 完成事件傳播可視化器
    - 點數：20
    - 稀有度：common

13. **偵探高手** 🔍
    - 完成事件對象解析器
    - 點數：20
    - 稀有度：common

14. **委託大師** ⚡
    - 完成事件委託演示器
    - 點數：25
    - 稀有度：rare

15. **自定義事件專家** 🎨
    - 完成自定義事件工作坊
    - 點數：30
    - 稀有度：rare

16. **教程通關** 🎓
    - 完成所有 4 個教程
    - 點數：60
    - 稀有度：epic

### 項目類成就（Projects）

17. **項目啟動** 🚀
    - 完成任意一個項目
    - 點數：20
    - 稀有度：common

18. **TODO 大師** 📋
    - 完成 TODO List Pro
    - 點數：30
    - 稀有度：rare

19. **輪播專家** 🖼️
    - 完成圖片輪播器
    - 點數：30
    - 稀有度：rare

20. **菜單大師** 🎯
    - 完成下拉菜單系統
    - 點數：35
    - 稀有度：epic

21. **項目全通** 💎
    - 完成所有 3 個項目
    - 點數：70
    - 稀有度：epic

### 綜合成就（Overall）

22. **初學者** 🌟
    - 總點數達到 50
    - 稀有度：common

23. **進階學習者** 🌟🌟
    - 總點數達到 150
    - 稀有度：rare

24. **專家** 🌟🌟🌟
    - 總點數達到 300
    - 稀有度：epic

25. **DOM 大師** 👑
    - 完成所有 12 個模塊
    - 點數：100
    - 稀有度：legendary

26. **完美主義者** 💯
    - 解鎖所有成就
    - 點數：150
    - 稀有度：legendary

27. **連續學習** 🔥
    - 連續 3 天訪問學習中心
    - 點數：25
    - 稀有度：rare

28. **夜貓子** 🦉
    - 在凌晨 0:00-6:00 完成任意模塊
    - 點數：15
    - 稀有度：common

29. **速度挑戰** ⚡⚡
    - 一天內完成 5 個模塊
    - 點數：50
    - 稀有度：epic

30. **探索者** 🔭
    - 訪問所有 12 個模塊（不需完成）
    - 點數：20
    - 稀有度：common

## 技術實現

### 文件結構
```
js/learning/gamification/
├── achievements.js        // 成就系統核心邏輯
├── achievementPopup.js    // 成就彈窗組件
├── progress.js            // 進度追蹤系統
├── progressRing.js        // 環形進度圖組件
├── leaderboard.js         // 排行榜系統
└── storage.js             // localStorage 封裝
```

### 核心 API

#### achievements.js
```javascript
class AchievementSystem {
  constructor()

  // 初始化系統
  init()

  // 記錄事件（自動檢查成就）
  trackEvent(eventType, data)

  // 檢查並解鎖成就
  checkAchievements()

  // 手動解鎖成就
  unlockAchievement(achievementId)

  // 獲取所有成就
  getAllAchievements()

  // 獲取已解鎖成就
  getUnlockedAchievements()

  // 獲取用戶統計
  getUserStats()

  // 重置進度
  resetProgress()
}
```

#### achievementPopup.js
```javascript
class AchievementPopup {
  constructor(container)

  // 顯示成就彈窗
  show(achievement)

  // 隱藏彈窗
  hide()

  // 渲染彈窗 HTML
  render(achievement)
}
```

#### progress.js
```javascript
class ProgressTracker {
  constructor()

  // 記錄模塊完成
  completeModule(moduleId, score)

  // 獲取模塊進度
  getModuleProgress(moduleId)

  // 獲取總體進度百分比
  getOverallProgress()

  // 獲取分類進度
  getCategoryProgress(category)
}
```

#### progressRing.js
```javascript
class ProgressRing {
  constructor(container, options)

  // 設置進度值（0-100）
  setProgress(percentage)

  // 渲染環形圖
  render()

  // 動畫更新
  animateProgress(fromValue, toValue)
}
```

#### leaderboard.js
```javascript
class Leaderboard {
  constructor(container)

  // 添加分數記錄
  addScore(playerName, points, achievements)

  // 獲取排行榜
  getLeaderboard(limit)

  // 渲染排行榜
  render()

  // 清空排行榜
  clear()
}
```

## 集成方案

### 1. 在每個模塊中集成

每個遊戲/教程/項目模塊需要：

```javascript
import { achievementSystem } from './gamification/achievements.js';

// 模塊完成時
onModuleComplete(score) {
  achievementSystem.trackEvent('module-complete', {
    moduleId: 'whack-a-mole',
    category: 'games',
    score: score,
    timestamp: Date.now()
  });
}
```

### 2. 在 learn.html 中顯示

添加成就和進度面板：
- 頂部顯示總體進度環形圖
- 側邊欄顯示最近解鎖的成就
- 底部顯示排行榜

### 3. 事件類型

系統監聽的事件：
- `module-complete` - 模塊完成
- `module-visit` - 訪問模塊
- `score-achieved` - 達成分數
- `time-record` - 時間記錄
- `session-start` - 會話開始
- `session-end` - 會話結束

## 視覺設計

### 稀有度配色
- **Common（普通）**: 灰色 `#95a5a6`
- **Rare（稀有）**: 藍色 `#3498db`
- **Epic（史詩）**: 紫色 `#9b59b6`
- **Legendary（傳說）**: 金色 `#f39c12`

### 動畫效果
- 成就解鎖：從底部滑入 + 淡入
- 進度更新：數字滾動動畫
- 環形圖：平滑過渡填充

## 數據持久化

使用 localStorage 存儲：
- `vdp_achievements` - 成就數據
- `vdp_progress` - 進度數據
- `vdp_leaderboard` - 排行榜數據
- `vdp_user_stats` - 用戶統計

## 預計代碼量

- `achievements.js`: ~400 行
- `achievementPopup.js`: ~200 行
- `progress.js`: ~250 行
- `progressRing.js`: ~150 行
- `leaderboard.js`: ~200 行
- `storage.js`: ~100 行
- CSS: ~300 行

**總計**: ~1,600 行代碼

## 開發順序

1. ✅ 設計系統架構和數據結構
2. 實現 storage.js（localStorage 封裝）
3. 實現 achievements.js（核心邏輯）
4. 實現 achievementPopup.js（彈窗組件）
5. 實現 progress.js（進度追蹤）
6. 實現 progressRing.js（環形圖）
7. 實現 leaderboard.js（排行榜）
8. 集成到所有模塊
9. 更新 learn.html 顯示成就面板
10. 測試和調試
